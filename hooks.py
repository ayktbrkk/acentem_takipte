# NOTE: Frappe does not load this repository-root hooks file.
#
# Canonical runtime hooks live in:
#   acentem_takipte/hooks.py
#
# Keep this file as a lightweight pointer only. If you need to change
# doc_events, scheduler_events, boot hooks, or translated languages, edit the
# canonical file above and validate the change there.
#
# This avoids keeping a second full hooks copy in sync and removes an easy
# source of config drift.


